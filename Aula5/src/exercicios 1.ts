// Exercicio 1.1
function maiuscula(f: string) : string {
    return f.toUpperCase()
} 
console.log(maiuscula("Anna Livia"));

// Exercicio 1.2

const maiusculaLambda = (f: string) : string => f;

console.log(maiuscula("Anna Livia"));